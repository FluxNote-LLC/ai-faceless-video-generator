{
  "name": "fluxnote-faceless-video-examples",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": { "test": "python3 -m unittest discover -s tests -v && node --test tests/javascript.test.mjs" }
}
